[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: requires a live ArcadeDB instance (deselect with '-m "not integration"')
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
